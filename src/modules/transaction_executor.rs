pub struct TransactionExecutor {}

impl TransactionExecutor {
    pub fn new() -> Self {
        Self {  }
    }

    pub async fn run(&self) {
        loop {
            // Update blockchain data
            // Update shared state
        }
    }
}